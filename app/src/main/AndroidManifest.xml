<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Appwriteandroidtrae">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".BankStatsActivity"
            android:exported="false"
            android:label="@string/feature_bank"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".SubscriptionActivity"
            android:exported="false"
            android:label="@string/feature_subscription"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".FoodManagementActivity"
            android:exported="false"
            android:label="@string/feature_food"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".FengNotesActivity"
            android:exported="false"
            android:label="@string/feature_notes"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".FengCommonActivity"
            android:exported="false"
            android:label="@string/feature_common"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".LotteryReasonActivity"
            android:exported="false"
            android:label="@string/feature_lottery_reason"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".OilMonitorActivity"
            android:exported="false"
            android:label="@string/feature_oil"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".USDebtActivity"
            android:exported="false"
            android:label="@string/feature_us_debt"
            android:parentActivityName=".MainActivity" />

        <receiver
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
